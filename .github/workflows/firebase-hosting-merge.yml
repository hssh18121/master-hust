# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - prod
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - run: npm ci && npm run build

      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_MASTER_HUST }}'
          channelId: live
          projectId: master-hust

      - name: Notify success on Teams
        if: success()
        run: |
          curl --location '${{ secrets.TEAMS_WEBHOOK_URL }}' \
          --header 'Content-Type: application/json' \
          --data '{
            "type": "message",
            "attachments": [
              {
                "contentType": "application/vnd.microsoft.card.adaptive",
                "content": {
                  "$schema": "http://adaptivecards.io/schemas/adaptive-card.json",
                  "type": "AdaptiveCard",
                  "version": "1.2",
                  "body": [
                    {
                      "type": "ColumnSet",
                      "columns": [
                        {
                          "type": "Column",
                          "width": "stretch",
                          "items": [
                            {
                              "type": "TextBlock",
                              "text": "🎉 ${{ github.repository }} のブランチ ${{ github.ref_name }} でのビルドとデプロイが成功しました。",
                              "wrap": true
                            },
                            {
                              "type": "TextBlock",
                              "text": "<at>{{ github.actor }}</at> がビルドを実行しました。",
                              "wrap": true
                            },
                            {
                              "type": "TextBlock",
                              "text": "ランの詳細は[こちら](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})をご覧ください。",
                              "wrap": true
                            }
                          ]
                        }
                      ]
                    }
                  ],
                  "msteams": {
                    "entities": [
                      {
                        "type": "mention",
                        "text": "<at>${{ github.actor }}</at>",
                        "mentioned": {
                          "id": "${{ github.actor }}",
                          "name": "${{ github.actor }}"
                        }
                      }
                    ]
                  }
                }
              }
            ]
          }'

      - name: Notify failure on Teams
        if: failure()
        run: |
          curl --location '${{ secrets.TEAMS_WEBHOOK_URL }}' \
          --header 'Content-Type: application/json' \
          --data '{
            "type": "message",
            "attachments": [
              {
                "contentType": "application/vnd.microsoft.card.adaptive",
                "content": {
                  "$schema": "http://adaptivecards.io/schemas/adaptive-card.json",
                  "type": "AdaptiveCard",
                  "version": "1.2",
                  "body": [
                    {
                      "type": "ColumnSet",
                      "columns": [
                        {
                          "type": "Column",
                          "width": "stretch",
                          "items": [
                            {
                              "type": "TextBlock",
                              "text": "🚨 ${{ github.repository }} のブランチ ${{ github.ref_name }} でのビルドとデプロイが失敗しました。",
                              "wrap": true
                            },
                            {
                              "type": "TextBlock",
                              "text": "<at>{{ github.actor }}</at> がビルドを実行しました。",
                              "wrap": true
                            },
                            {
                              "type": "TextBlock",
                              "text": "ランの詳細は[こちら](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})をご覧ください。",
                              "wrap": true
                            }
                          ]
                        }
                      ]
                    }
                  ],
                  "msteams": {
                    "entities": [
                      {
                        "type": "mention",
                        "text": "<at>${{ github.actor }}</at>",
                        "mentioned": {
                          "id": "${{ github.actor }}",
                          "name": "${{ github.actor }}"
                        }
                      }
                    ]
                  }
                }
              }
            ]
          }'
